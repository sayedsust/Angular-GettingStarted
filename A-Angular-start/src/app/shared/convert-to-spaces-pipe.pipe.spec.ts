import { ConvertToSpacesPipePipe } from './convert-to-spaces-pipe.pipe';

describe('ConvertToSpacesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToSpacesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
